using UnityEngine;
using static MyFunClasses.TestClass;

public class Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start() {
        PrintStuff();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
